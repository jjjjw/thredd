var Comments = require('./comments')
  , keyById = require('transforms').keyById
  , React = require('react/addons')
  ;

// Maybe org under views/templates?
module.exports = function (comments, users) {
  return React.renderComponentToString(Comments({comments: comments, users: keyById(users)}));
};
