var React = require('react');

module.exports = React.createClass({
  getInitialState: function () {
    return {user: null};
  }
  , render: function () {
    var currentUser = this.state.user;
    var userName = this.state.user ? this.state.user.name : 'Anonymous';

    return React.DOM.span({ className: 'user' }, userName);
  }
});
